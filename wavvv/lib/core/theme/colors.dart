import 'package:flutter/material.dart';

class WavvvColors {
  static const bg = Color(0xFF080808);
  static const bgCard = Color(0xFF0F0F0F);
  static const bgGlass = Color(0x0DFFFFFF); // 5% white
  static const bgInput = Color(0xFF141414);
  static const glassBorder = Color(0x1AFFFFFF); // 10% white
  static const glassBorderHover = Color(0x33FFFFFF); // 20% white
  static const red = Color(0xFFFF0000);
  static const redDark = Color(0xFFCC0000);
  static const redGlow = Color(0x40FF0000); // 25% red
  static const wave = Color(0xFF6366F1); // indigo
  static const waveGlow = Color(0x406366F1); // 25% indigo
  static const textPrimary = Color(0xFFFFFFFF);
  static const textSecondary = Color(0xFF888888);
  static const textMuted = Color(0xFF444444);
  static const online = Color(0xFF22C55E);
  static const synced = Color(0xFF6366F1);
  static const host = Color(0xFFFFBF00);
}
